{
  "name": "markdown-rbmk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "RBMK reactor-core style SVG badge for GitHub language stats",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:fn": "ncc build packages/server/api/badge.ts -o .fn-build --minify --transpile-only && cp .fn-build/index.js badge-bundle.js && rm -rf .fn-build",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "pnpm --filter @markdown-rbmk/core preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vercel/ncc": "^0.38.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
