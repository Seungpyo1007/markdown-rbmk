{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'skip — the function bundle is prebuilt (pnpm build:fn)'",
  "outputDirectory": "public",
  "functions": {
    "api/badge.ts": { "maxDuration": 30 }
  }
}
